# Copy this file to .env and fill in your SMTP credentials to enable email notifications
# SMTP server host (e.g., smtp.gmail.com)
SMTP_HOST=
# SMTP server port (587 for TLS)
SMTP_PORT=587
# Use secure SSL/TLS? set to 'true' for port 465
SMTP_SECURE=false
# SMTP auth user and pass
SMTP_USER=
SMTP_PASS=
# Optional: email address that will receive contact form submissions (defaults to SMTP_USER)
CONTACT_RECIPIENT=
# Optional: from address shown in emails
EMAIL_FROM=Deepak <no-reply@example.com>
