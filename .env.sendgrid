# Environment variables for Netlify Functions (SendGrid email)
# Copy these to Netlify Site Settings → Environment

# SendGrid API key (get from sendgrid.com → Settings → API Keys)
SENDGRID_API_KEY=your_sendgrid_api_key_here

# SendGrid from email (must be verified in SendGrid)
SENDGRID_FROM_EMAIL=no-reply@yourdomain.com

# Where contact submissions are sent (optional; defaults to SENDGRID_FROM_EMAIL)
CONTACT_RECIPIENT=your-email@example.com
